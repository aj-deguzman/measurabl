SELECT S.ID SITE_ID, S.NAME SITE_NAME, S.ADDRESS, S.CITY, 
S.STATE, S.ZIPCODE, U.DESCRIPTION, T.NAME TYPE
FROM SITES S
LEFT JOIN SITE_USES U
 ON U.SITE_ID = S.ID
LEFT JOIN USE_TYPES T
 ON T.ID = U.USE_TYPE_ID
WHERE S.CITY = 'San Diego'